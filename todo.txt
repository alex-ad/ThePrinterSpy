	+1) Обновление totalstat во всех методах
	+2) Отчет : дата
	-3) Текст периода отчета ведет в настройки
4) Сервис : дата в БД
	+5) ПА: сравнение строк в имени принтера - заменить на стандартные функции
	+6) Сервис: убрать из цикла список принтеров, оставить только при запуске; в цикле - если есть новый (определим его из задания печати), просто добавить
	+7) Конфиг: валидация данных. (тип поля ввода (или просто проверка) маски имени принтера зависит от типа маски)
8) Конфиг: проверка соединения с БД при изменении (кнопка отдельная)
9) +настройку время хранения записей в БД. кнопки "очистка БД"
10) +настройку версии БД и ПО (для проверки совместимости)
	-11) +Lazy<> ко всем спискам (троелсен, стр. 504)
	+12) Сервис: Проверка (из таблицы опций в БД), нужно ли брать инфу из АД, перед получением этой инфы
13) Сервис: при эксепшине - стоп
	+14) ПА: если БД пустая, вылетают ошибки - исправить
14) ЛОГИ: каждая ошибка - с новой строки (\r\n не работает?)
15) Сервис: отслеживать добавление/удаление принтера ... или тупо делать опрос локальных принтеров в цикле
16) Сервис: когда в БД добавляются принтеры локальные, фильтровать по имени, т.е отключать все pdf, xps, virtual, onenote,...
	+17) ПА: контекстное меню принтеров - вкл/выкл, удалить из БД, переименовать
	-18) ПА: роли пользователя на просмотр/редактирование
	+19) ПА: если элемент (например, в списке на вкладке Принтеры) один, то не получается его выбрать, т.е не срабатывает событие по клику. Решение: 1) добавить событие клик (хреново); 2) добавить корневой элемент, получится подчиненный список (норм)
	+20) ПА: не работать напрямую с БД. Изменяем модель (через статические методы), а модель уже меняет БД. Если меняем принтер (вкл-выкл, имя), в СелектедПринтер меняем состяние принтера, а уже оттуда обновляются модели принтеров в юзерах и компьютерах, а также обновляется БД, а также отууда же обновляется ТоталСтат. То же при удалении принтера. Для этого в СелектедПринтер и ТоталСтат создаем статические методы.
	+21) ПА: при переключении вкладок сбрасывать SelectedPrinter
	+22) ПА : кнопки "синхра с АД"
	+23) ПА : при синхре с АД, два раза GetUser, надо один
	+24) все сравнения строк заменить на "без культуры"
	-25) +mysql
26) Сервис : имя принтера берется с именем компа, нужно просто имя
27) Сервис : Расщаренный принтер, сервер совпадает с компом - неправильно
28) Сервис : в БД постоянно создается "новый" пользователь при каждом запуске